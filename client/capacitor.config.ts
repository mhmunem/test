import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.grocery_comparison.app',
    appName: 'grocery_comparison',
    webDir: 'dist'
};

export default config;
